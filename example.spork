+add[sys]("std")[std]

func add[none](int a, int b)[none] int {
    return a + b
}

func main[none](string[] args)[] {
    var thing int = add(1, 2)
    std.io.println("1 + 2 is {}")[{thing}]
    if (add(1,3) > 1) {
        return 0
    } else {
        return(20)
    }
}
